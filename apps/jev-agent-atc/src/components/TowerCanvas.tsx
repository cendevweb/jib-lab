/**
 * Control-tower canvas (SPEC §6).
 * CONTRACT STUB (harness): renders nothing until WP-03 implements it. Contract return type is
 * ReactElement; `| null` only exists for the stub.
 */
import type { ReactElement } from "react";
import type { TowerFrame } from "@/domain/types";

export function TowerCanvas(_props: { frame: TowerFrame; t: number }): ReactElement | null {
  return null;
}
