/**
 * Latest decision + shared state panels (SPEC §6).
 * CONTRACT STUB (harness): render nothing until WP-03 implements them. Contract return type is
 * ReactElement; `| null` only exists for the stub.
 */
import type { DecisionRecord } from "@jib/jev";
import type { ReactElement } from "react";

export function DecisionSpotlight(_props: {
  record: DecisionRecord | undefined;
}): ReactElement | null {
  return null;
}

export function SharedStatePanel(_props: {
  record: DecisionRecord | undefined;
}): ReactElement | null {
  return null;
}
